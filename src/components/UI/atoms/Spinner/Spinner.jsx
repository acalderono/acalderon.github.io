import React from "react";
import { Container, Circle } from "./Styles.jsx";

export const Spinner = () => {
  return (
    <Container>
      <Circle></Circle>
    </Container>
  );
};

export default Spinner;
